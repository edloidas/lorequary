# @lorequary/web

The [Lorequary](https://github.com/edloidas/lorequary) editor — a React 19 SPA, private to the workspace.

Everything runs client-side: no server, no auth. Projects autosave to IndexedDB and export as `.lorequary` files or runtime JSON IR. Run it with `pnpm dev` from the repo root.
